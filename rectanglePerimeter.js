function rectanglePerimeter(length, width) {
    return 2 * (length + width);
}

var length = 5;
var width = 3;
var perimeter = rectanglePerimeter(length, width);
console.log("მართკუთხედის პერიმეტრია:", perimeter);
